# Release Log

Use one line per shipped change.

| Date | Branch | Commit | Type | Summary | AI Tool | AI Usage | Human Check | Validation | Client Impact | Rollback | Defect Found |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-03-20 | feat/fastapi-backend | uncommitted | feat | Extracted the desktop monolith into a package-based runtime, added an async FastAPI job/export backend with managed workspace storage, cache reuse, tests, and updated docs; Snyk unavailable | codex | code | self-verified | Validation completed with `venv/bin/python -m py_compile main.py athleo/desktop.py athleo/models.py athleo/storage.py athleo/backend.py athleo/jobs.py athleo/api/app.py athleo/api/schemas.py athleo/api/routes/jobs.py tests/test_api.py`, `venv/bin/python -m unittest discover -s tests`, and `venv/bin/python main.py --help` | Athleo now supports backend-first upload/process/poll/export workflows through FastAPI while preserving the desktop player on the extracted shared runtime | Restore the prior package structure, `main.py`, `requirements.txt`, `README.md`, and docs state | None |
| 2026-03-20 | feat/multi-link-text-area-modes | uncommitted | feat | Expanded pair-link into ordered multi-link mode and added moving player text labels plus transparent player-area polygon mode; no dependency changes required | codex | code | self-verified | Manual validation completed with `py_compile`, `main.py --help`, and local render assertions covering multi-link skipping, text labels, area polygon rendering, and export path generation | New `P`, `T`, and `G` workflows add ordered multi-player chains, player-attached text labels, and dynamic transparent selected-player areas in playback and export | Restore the prior `main.py`, `README.md`, and docs state | None |
| 2026-03-19 | feat/pose-zoom-ball-modes | uncommitted | feat | Added cached pose-skeleton, zoom-follow, and fireball ball-selection modes with export support; no dependency changes required | codex | code | self-verified | Manual validation completed with `py_compile`, `main.py --help`, and a repo-venv smoke run on a 24-frame `/tmp/athleo_smoke.mp4` clip covering fresh cache generation, cache reuse, and pose/zoom/fireball exports | New `K`, `Z`, and `B` modes add pose skeleton overlays, player zoom follow, and selectable fireball ball effects in playback and export | Restore the prior `main.py`, `README.md`, and docs state | None |
| 2026-03-19 | feat/player-overlay-modes | uncommitted | feat | Restyled pair-link mode with yellow player boxes, red broadcast-style ground rings, and a red anchor-to-anchor link; Snyk unavailable | codex | code | self-verified | Manual validation completed with `py_compile` plus pair-mode rendering assertions in a local Python snippet | Pair-link mode now matches the broadcast-style selected-player treatment more closely in playback and export | Restore the prior `main.py`, `README.md`, and docs state | None |
| 2026-03-19 | feat/player-overlay-modes | uncommitted | feat | Added spotlight and pair-link overlay modes with mode-aware export in the interactive player; Snyk unavailable | codex | code | self-verified | Manual validation completed with `py_compile` plus renderer/export state smoke checks in a local Python snippet | New `S` spotlight and `P` pair-link modes for live playback and export | Restore the prior `main.py`, `README.md`, and docs state | None |
| 2026-03-18 | feat/player-tracking-poc | uncommitted | feat | Refactored the desktop workflow into cached interactive playback with instant click-based player isolation and export-from-player; Snyk unavailable | codex | code | self-verified | Manual validation completed with `py_compile`, `--help`, click-hit/drawing assertions, and a full cached selected-player export smoke test to `/tmp/athleo_validation_selected.mp4` | Live-feeling local player that preprocesses once, reuses cached tracking data, and switches overlays immediately during playback | Restore the prior `main.py`, `README.md`, `requirements.txt`, and docs state | None |
| 2026-03-18 | feat/player-tracking-poc | uncommitted | feat | Added a local OpenCV desktop POC for YOLO26x soccer player tracking, preview, and selected-player export; Snyk unavailable | codex | code | self-verified | Manual validation completed with `py_compile`, `--help`, and a 30-frame smoke test covering cache reuse plus all-player and selected-player exports in the repo venv | New desktop player tracking workflow with cached metadata and export modes | Remove `main.py`, `requirements.txt`, and docs changes | None |
